//returns character at the given index of the string or empty string '' if index is out of bounds
let c = "helloworld".charAt(0);
console.log (c) //h

let str = "helloworld"
console.log (str.charAt(str.length-1)) //d
console.log (str.charAt(10)) // returns ''
